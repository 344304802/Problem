# 题目遗漏分析 — 对照 A 题原文检查

> 逐条对照题目原文，列出当前模型未考虑或未充分讨论的因素，给出补救方案与报告标注建议。

## 1. 二次电流（核心可调参数，数据缺失）

**题目原文**："各电场二次电压、**二次电流**、振打周期为核心可调参数，直接决定除尘效果与电能消耗。"

**当前模型**：数据无电流字段（`I1`~`I4`），电耗模型 $$P=\sum k_i U_i^2 + \sum \beta_i/T_i + c$$ 只用电压，未单独建模电流。

**处理方式：假设调节二次电压等价于调节二次电流**

在电晕放电工作区，二次电压与二次电流通过伏安特性 $$I = f(U)$$ 单调递增一一对应。因此"调节二次电压"在可控性上等价于"调节二次电流"——升电压则电流同步升，降电压则电流同步降，操作人员通过控制电压即可间接控制电流。

电耗模型中 $$U_i^2$$ 实际是 $$U_i \cdot I_i$$ 在 $$I_i \propto U_i$$（伏安特性线性区）假设下的近似，已隐含电流效应。**新增假设 H8**：

> **H8（电压-电流等价）**：电除尘器工作在电晕放电伏安特性单调区，二次电压与二次电流一一对应，调节二次电压等价于调节二次电流，不单独建模电流。

**报告标注**：在假设列表中写明 H8，在电耗模型公式处注明"$$U^2$$ 近似 $$U\cdot I$$，隐含 $$I\propto U$$"。无需额外补救或估算。

## 2. 粉尘比电阻（随工况变化，数据缺失）

**题目原文**："粉尘比电阻也随工况实时变化。"

**当前模型**：数据无比电阻字段。Deutsch 方程中比电阻隐含在拟合参数 $$kA_0$$ 中，未显式建模。

**处理方式：不单独建模，标注影响隐含在 $$kA_0$$**

粉尘比电阻影响驱进速度 $$\omega$$（高比电阻导致反电晕、驱进速度降），但该效应在拟合 $$kA_0$$ 时已被**隐式吸收**——$$kA_0$$ 偏小即反映高比电阻工况的平均影响。数据无比电阻字段，无法建立 $$\omega = \omega_0 \cdot g(\rho)$$ 的显式关系，强建只会引入无数据支撑的参数。

**报告标注**：在讨论部分注明"比电阻影响隐含在 $$kA_0$$ 中，$$kA_0$$ 的不确定性部分来自比电阻未显式建模；后电场 $$\alpha_3,\alpha_4$$ 不可辨识可能与此有关"。无需额外补救。

## 3. 振打同步性（多电场峰值叠加，问题1要求）

**题目原文**：问题1"研究振打周期对瞬时排放峰值（'振打尘饼'导致的短时浓度飙升）的影响。"

**当前模型**：`predict_peak` 假设各电场独立叠加 $$C_{peak} = \sum \alpha_i d_i C_{in}\cdot 1000 \cdot 0.1$$，未考虑多电场**同时振打**的同步叠加效应。

**物理影响**：若 $$T_1=T_2=T_3=T_4$$（周期相同），四电场同时振打，瞬时峰值是单电场的近 4 倍（最坏情况）。错峰振打（$$T_i$$ 互质或错开相位）可大幅降低峰值。这是工程上重要的协同控制策略。

**补救**：
- 分析数据中 $$T_1\text{-}T_4$$ 的同步性（是否经常相同）
- 建模同步因子 $$S = \frac{(\sum \delta_i)^2}{\sum \delta_i^2}$$（$$\delta_i$$ 为各电场振打时刻偏差），$$S=4$$ 完全同步，$$S=1$$ 完全错峰
- 优化建议加"错峰振打"约束：$$T_i \neq T_j$$ 或相位错开

## 4. 稳定运行（题目三目标之一，暂不单独建模）

**题目原文**："无法实现环保达标、能耗最低、**稳定运行**的协同目标。"

**当前模型**：以电耗最低（目标）+ 排放达标（约束）为主，未单独建模"稳定运行"。

**处理方式：在报告中讨论，暂不大动干戈**

稳定运行的物理含义包括参数波动小、控制平滑、抗扰动。当前优化结果的最优参数在工况间差异合理（未剧烈偏离历史运行域），已隐含一定的稳定性。如需显式建模可加正则项 $$\lambda\sum(U_i - U_i^{hist})^2$$，但当前不单独建模。

**报告标注**：在讨论部分注明"当前以电耗+排放为主目标，稳定运行作为三目标权衡的讨论点；鲁棒优化（机会约束）已部分回应抗扰动需求"。

## 5. 工况划分变量（题目措辞 vs 实际）

**题目原文**："考虑到工况（入口浓度、温度）随时间波动。"

**当前模型**：K-Means 用 $$(C_{in}, T_{in}, Q)$$ 三个变量。

**分析**：题目只提浓度和温度，但流量 $$Q$$ 也是重要工况变量（直接影响 Deutsch 方程 $$\eta = 1-\exp(-kA\cdot U^2/Q)$$，$$Q$$ 大则效率降）。加入 $$Q$$ 合理，但应在报告中说明：
- 题目措辞是举例（"如"），非穷举
- $$Q$$ 对效率有一阶影响（Sobol $$S_Q$$ 应非零），排除 $$Q$$ 会丢失工况差异
- 可补做：只用 $$(C_{in}, T_{in})$$ 划分的对比，看 $$Q$$ 的影响

## 6. 时间序列特性（未利用）

**题目原文**："连续 7 天的运行数据""随原料波动、错峰生产、余热发电介入呈现剧烈时序波动"。

**当前模型**：除时序交叉验证外，未利用时间序列结构。

**可做**：
- 识别日内周期（白天生产/夜间错峰）、趋势分量
- 检测工况突变点（余热发电介入时刻）
- 自相关分析：$$C_{out}$$ 的 AR 结构，残差是否白噪声
- 在报告中画 7 天时序图，标注工况切换

## 7. 电场间耦合（协同控制）

**题目原文**：标题"**协同**优化控制"。

**当前模型**：Deutsch 串联假设各级独立（$$H_1$$），前级出口即后级入口，但未建模：
- 前电场振打释放的粉尘**重返气流**进入后电场（使后电场负荷突增）
- 前电场电压影响后电场空间电荷（电场屏蔽）

**补救**：在报告中讨论 $$H_1$$ 的局限，标注串联独立假设在振打瞬间不成立（振打释放粉尘破坏级间独立性）。

---

## 报告标注建议汇总

| 遗漏项 | 严重度 | 报告处理 |
|--------|--------|----------|
| 二次电流 | 低 | 假设 H8：调节电压等价调节电流（伏安特性单调区），不单独建模 |
| 比电阻 | 低 | 影响隐含在 $$kA_0$$ 中，标注不确定性来源，不显式建模 |
| 振打同步 | 高 | 补同步性分析 + 错峰振打建议（工程价值大） |
| 稳定运行 | 低 | 讨论三目标权衡，鲁棒优化已部分回应，暂不单独建模 |
| 工况划分变量 | 低 | 说明加 $$Q$$ 的合理性，补对比 |
| 时间序列 | 低 | 画时序图，讨论周期/趋势 |
| 电场间耦合 | 低 | 讨论 $$H_1$$ 在振打瞬间不成立 |